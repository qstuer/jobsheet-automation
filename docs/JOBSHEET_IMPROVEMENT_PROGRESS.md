# Jobsheet 辨認改善進度

只記錄改動與匿名驗收結果，不保存客戶、Serial、電話、Asana task GID 或私人答案。
主要系統手冊仍是根目錄 `CLAUDE.md`。

## 執行約定

- 一次完成一個可驗證的小步驟，展示結果再繼續；不同規則不混在一次改動內。
- 開發／回測不寫 OneDrive、不移動 Google Drive 工作單、不更換正式模型。
- 先展示 diff；提交、推送與正式啟用另行確認。
- 真實答案必須來自原件與獨立 Asana 核對，不能把受測配對器的結果抄成答案。

## 分步清單

### 2026-09-23：獨立分支真實回測準備

- 用戶已授權僅推送 `codex/backtest-reviewed-20260923` 作20份只讀回測，main 保持不變。
- 本次包含截至5A的本機改動；4B聯合知識提示仍未接入正式匹配入口，5B日期後備仍未完成，不能宣稱本輪涵蓋全部計劃。
- 使用另存的已覆核 schema 2 私人清單，19份工作答案就緒，1份原件衝突未確認；三份缺頁只作診斷，不算成品通過。同工作不同掃描版本另外標記。
- 答案及PDF不入Git；workflow下載後先驗證答案清單和各PDF指紋。舊私人答案不覆蓋。
- 只在此測試分支啟動，使用既有模型變數及Secrets；不更換正式OCR_PROVIDER。
- 回測入口禁止rclone及正式上傳函數；逐份保存匿名判分、用量及耗時，外部錯誤不公開原文或traceback。結果保留7天，不上傳客戶內容。
- 本機245個自動測試通過（包括新增的防雲端寫入、匿名中斷處理及結果保存測試）；這不是20份實單準確率。真實結果待workflow執行後另行記錄。

| 階段 | 內容 | 狀態 |
|---|---|---|
| 1A | 修正回測判分、匿名分項報告、只讀樣本盤點 | 本機完成；未提交／未推送 |
| 1B | 核實正確 task、名稱、類型、原始日期及重複工作 | 已盤點全部20份：19份確認工作，1份跨頁衝突未確認；原始上傳日期未查證 |
| 2 | 檢查裁切及放大效果；真正測試 CM／PM 圈選 | 本機修正及20份圖片人工檢查完成；付費模型圈選／欄位實測未執行 |
| 3 | 保留原始讀數／拒絕原因；產品不補造、日期格式統一 | 本機完成；203個自動測試通過，尚未真實模型回測／部署 |
| 4A | 清理索引產品欄，建立已確認的小型產品詞表 | 本機解析修正及私人小表完成；215個測試通過，舊雲端索引尚未重建 |
| 4B | 同看產品與 Serial；加入實際規律，對照測試有無提示 | 私人規律／聯合讀取／同圖對照入口完成，229個測試通過；真實模型對照待可用密鑰，未接正式流程 |
| 5 | Asset 只加分；工作日期後備及類型核對 | 未開始 |
| 6 | 完整私人回測；確認後單份正式觀察 | 未開始 |

已確認的第五步要求（尚未實作）：日期讀不清才考慮原始上傳日前 70 天至後 30 天；
Asset 相似率四捨五入至整數後達 70% 才加分，缺失／不符不扣分、不設必需門檻。
ACTION DATE 與 CM／PM 的明確衝突不能被「最近日期」蓋過。

## 2026-09-17：1A 回測驗收工具

起始提交：`858d17d`。只改回測及文件，沒有修改正式 OCR、Asana 配對或 workflow。

### 發現與修正

- 舊版 `serial` 答案只核對 Serial；同一設備另一月份的 task 也會通過。
- 完整配對答案要求 Serial、task GID 與最終 PDF 名稱全部吻合，Asana 類型也須吻合。
- 舊 schema 1 可讀，但只提供診斷，結果為 `UNVERIFIED`；不能當完整通過率。
- schema 2 區分已確認／未覆核答案，保存獨立覆核依據與原始日期來源；未知日期保持 null。
- 分列設備、工作、名稱、Asana 類型；欄位準確率、圈選與全流程標為 `NOT_TESTED`。
- CM 非 1 頁或 PM 非 4 頁，即使配對正確也只算 `DIAGNOSTIC_ONLY`。頁數吻合只是 `COUNT_ONLY`，不是 checklist 完整驗證。
- 同 task 的不同 PDF 標為重複工作；沒有 task 答案就標身份未知，不能據 SHA 不同宣稱工作互異。
- 新增 `--audit-fixtures` 本機盤點模式，不呼叫 OCR、Asana 或雲端操作。

### 真實資料盤點（非 OCR 重跑）

- 共 20 份；17 份為 4 頁，B16、B17、B18 各 3 頁；没有完全相同的 PDF。
- 私人清單仍是 schema 1：16 份 Serial 答案、3 份訂單號答案、1 份預期待確認。
- 20 份均未保存新版要求的已覆核完整答案；20 份沒有原始日期欄位。
- 「未核验」是答案清單不足，不是宣稱 20 份全部配錯。
- 不把舊 `pending` 答案當成已重新確認，須再次核對原件與工作。
- 未修改私人答案、PDF、設備索引或雲端資料；沒有模型呼叫或 API 測試費用。

### 驗證

- 回測自動測試：18 個通過（原 4 個，新增 14 個）。
- 全部自動測試：179 個通過。
- 涵蓋錯月份、錯檔名、錯設備、類型錯誤／未知、無訂單的 `/` 命名、缺頁、
  舊答案不得假通過、已確認 pending、安全路徑、未知日期及重複工作。
- 模擬整批執行確認不呼叫正式上傳／處理或 rclone 寫入功能。

### 下一個小步驟

完成 1B：從原件及獨立 Asana 任務讀取補齐私人答案，保留候選與已確認答案的區別。
未知原始日期不使用本機檔案修改時間或回測日頂替。
1B 未完成前不宣布全體回測達標，也不為通過數字反覆花費 OCR 呼叫。

之後才開始裁切檢查及產品詞表清理。目前索引的產品欄已有類型碼／序號片段混入，
未清理前不可整表當作模型的正確產品知識。

## 2026-09-17：1B 私人答案獨立覆核

### 已完成

- 直接從本機20份PDF重新渲染並查看首頁與第二頁身份欄；不沿用同名舊預覽。
- 使用Asana只讀工具独立讀取20個真實task（包括衝突候選），沒有呼叫受測配對器或付費OCR。
- 私人schema 2清單另存 `tmp/backtest-20-upload/manifest-reviewed.json`，不覆蓋舊清單。
- 19份保存核實後的task GID、最終檔名、PM圈選、ACTION DATE、原件差異及覆核依據。
- B05保留為 `unreviewed`，附候選及明確原因；不沿用舊版「必定pending」答案，也不把新候選算成功。
- 原始上傳／掃描時間尚未獨立查證，20份 `reference_date` 均保留null；ACTION DATE另外保存。
- B01/B16、B07/B17是同設備不同月份，使用不同task；B13/B20是同工作不同版本。
- 19份已確認樣本代表18筆不同工作；其中3份只有3頁。符合4頁數量且有確認答案的是16份、15筆不同工作，仍未完成逐頁完整性驗收。
- Asana證據及逐份報告保存在 `tmp/sample-review-20260917/`，已確認被Git忽略，沒有上傳私人答案到雲端。

### 新發現（不能歸咎於模型）

- B05首頁和checklist的Serial不同。Checklist支持另一個候選；兩個近似設備同院、同電話、正式日期同樣接近，日期本身不能決勝。原件跨頁身份衝突仍待確認。
- B13/B20首頁Serial與checklist不同；checklist、工作參考號、asset、電話、日期及B20打印訂單共同確認同一工作。不能把它描述成單純一字OCR誤差。
- 部分舊人工觀察腳本的日期、電話、asset也有錯，已在私人新清單記錄直接可見原文，不沿用舊腳本當正解。
- Asana描述亦存在多一位電話或不同電話；不是所有差異都由OCR造成。
- B18本機版本第2頁已是checklist第2頁，缺少checklist第1頁；不代表用户已確認的OneDrive版本有問題，本次沒有讀寫該成品。

### 本次小幅工具修正與驗證

- 私人已覆核清單加入 `source_sha256`，綁定實際PDF。檔名相同但內容變動時，回測會在任何OCR呼叫前停止，要求重新核驗。
- 增加原件指紋格式與替換檔案測試；完整自動測試181個通過，編譯檢查通過。
- 新清單只讀盤點通過：19個答案就緒、1個未確認，B20正確標出與B13重複。
- 沒有執行真實模型回測，不能據此宣稱模型19/20正確；沒有修改正式辨認規則、提交或推送。

### 第二步只讀初檢與下一步

- 已生成20份程式實際送出的欄位卡，抽查B01、B16、B18。
- 實測5x和6x的單格Serial圖最終都只有1200×310像素；放大渲染被固定卡片尺寸限制。
- 已查看的卡片中，醫院、電話等長欄位字體比Serial小很多。尚不能宣稱20份所有欄位均通過裁切驗收。
- 下一步逐份完成裁切檢查，再改圖像輸出與提示詞；圈選需實際辨認。
- Checklist身份欄具有首頁以外的重要證據。後續需要保留跨頁讀数及衝突，不可直接用第二頁覆蓋第一頁，也不能以日期相近強行解決。

## 2026-09-17：2 圖片輸出與圈選回測入口

### 本機改動（未提交／未推送）

- 欄位卡寬度跟隨渲染倍率：3x/4x/5x/6x分別1200/1600/2000/2400px；渲染及輸出上限均6x，非法倍率在渲染前拒絕。
- 對同一原件實測：5x單格Serial由1200×310改為2000×518，6x改為2400×620。
- 提示詞允許打印、打字和手寫值，仍禁止把欄名、鄰格或猜測內容當答案。
- 圈選必須是單獨且明確的選項；`not PM`等否定／解釋句不再被誤接受。保留最多一次放寬框重讀。
- 回測先實際讀取圈選。清單答案只作判分及停止條件，不能傳給模型當提示；配對器收到的是讀取結果。
- 圈選錯誤、UNKNOWN或非CM／PM停止該份配對；不能把「沒有配到」當作預期pending成功。報告加入圈選欄，用量合計包含圈選呼叫。
- 未改裁切座標、產品／醫院配對、Asset／日期規則、模型供應商、接力流程或防重複架構。

### 圖片人工檢查

- 從20份實際PDF產生程式首頁欄位卡及圈選裁切，逐份查看五張四合一圖、圈選總圖及單格放大樣本。
- 沒發現關鍵填寫內容整格被裁掉；醫院、聯絡人及電話因原欄位較寬而比Serial小。若干裁切邊緣有鄰格印刷字或上一行筆跡殘影，不能宣稱已完全消除鄰格干擾。
- 20份圈選人工均可辨為PM；這批沒有真實CM對照。B16/B17掃描字跡較弱，仍需模型實測。
- B18/B20的訂單號為打印值，證明只要求讀手寫會漏掉有效資料；本次已修提示詞。
- 私人圖片存放 `tmp/sample-review-20260917/step2/`，沒有雲端寫入。

### 驗證及限制

- 全部自動測試190個通過（本步新增9個）；涵蓋輸出像素上限、固定格分離、打印值提示、圈選否定句、一次重讀、錯誤停止及用量加總。
- 所有模型及雲端呼叫均使用測試替身；本次沒有付費OCR、Asana查詢或OneDrive寫入。
- 190個是程式測試，不代表真實20份模型識別通過。逐欄準確率、跨頁身份及缺頁攔截仍待驗收。
- 下一步是第三步：保留原始抄錄與拒絕原因，統一日期表示，停止替不完整產品名補造型號；之後清理產品小表，再加入產品／Serial雙向知識。

## 2026-09-18：3 原始抄錄、產品及日期整理

### 發現及改動

- 舊 `normalize_product` 以最近型號且先出現者作答案；`EPIQ` 因距離最短會變成 `EPIQ 7+`。現在大類可獨立保留；不增加型號數字／後綴，也不在並列時按清單順序猜。
- `EPLQ 5G`等唯一且保留數字／後綴的拼字校正仍可用；`Affiniti 70G`保留完整寫法，配對用的同款比較仍可與70相容。這不是私人產品詞表已完成。
- 原有 `*_raw` 欄其實經過安全閘，錯誤產品、醫院、日期會變成null，電話等列表會被過濾。新增 `_ocr_audit` 明確分開模型原文、整理後快照與拒絕原因；原欄位保持兼容且不能讀取診斷原文繞過安全閘。
- 訂單、Serial、產品、醫院、聯絡人、電話、Asset、WO和日期拒絕各有原因碼；超過候選數量上限亦保留完整原始列表。
- 多輪辨認保留首讀／身分複核／單格／輔助複核的階段、倍率和原文；Serial專用複核也不再只返回過濾後列表而遺失原文。
- 日期依真實日曆日比較，不再按去掉標點後的字串比較。`18/8/26`、`18/08/2026`和`2026-08-18`可形成共識，原字串另存；`service_date_iso`供Asana使用。
- 日/月/年按表格格式解讀，不改成美式月/日/年。不合法、超現行時間範圍或明確非ACTION DATE的值仍拒絕；本步沒有實作上傳日期後備、猜年份或放寬日期範圍。

### 驗證與邊界

- 新增13個合成資料測試；完整測試共203個通過。
- 涵蓋大類不補造、型號數字／後綴不改、同距離不猜、確認寫法、同日格式一致、不同日不一致、單輪不足、錯誤日期來源、拒絕值不復活、快照不受後續修改影響、候選上限、Serial專用複核與公開日誌保密。
- 私人原文目前只留在辨認回傳物件／記憶體；不自動保存到檔案、不寫公開Actions或雲端報告。之後私人回測報告需由本機測試入口明確保存，不能直接把整個OCR物件打印出來。
- 未呼叫真實OCR或Asana，未寫OneDrive，未移動Google Drive，未提交／推送。203個程式測試不代表真實20份準確率。
- 下一小步：清理Asana產品欄的CM／HAWO／Serial雜訊，整理私人產品小表；未清理前仍不把整個索引當成模型知識。之後才做產品／Serial雙向提示及有無提示對照。

## 2026-09-18：4A 產品解析與私人小表

### 本機舊資料發現

- 使用2026-09-16保存的schema 3索引：4,596個設備列、25,196個task計數，2,541種不同產品寫法。不是本次從Asana即時取得的資料。
- 舊解析器把Serial前一段或任意有字母的欄段當產品，令Serial片段、HAWO與人員文字進入產品欄。不能把這些「高頻產品」直接教給模型。
- 完整workspace包含多類設備。未確認寫法不是全部錯字，`CM10`／`CM12`這類含數字型號也不能當作CM工作類型刪掉。

### 改動

- 新增 `product_catalog.inspect_product`，分類為已確認型號、大類、未確認或拒絕。只有既有清單及確定同款寫法能進模型參考用的確認區，不用模糊校正或頻率替新型號認證。
- 改索引標題解析：先在Serial前找可确认產品；不從Serial後的工作類型／人員／編號補產品。無確認型號時，僅保留位於正常產品欄的合理未知寫法並標記未確認；明顯非產品文字及並列衝突不猜。
- 已確認產品與Serial在同一段時可分離；醫院／院內位置維持獨立，前置CM／HAWO欄不再併入地點。
- task參考新增產品解析狀態及原因碼；不加入完整標題、Order Number或外部密鑰。
- 增加產品解析版本，阻止增量刷新沿用舊污染欄；需要完整重建時，在任何Asana網路呼叫前停止並說明。現有正式Stage B未切換，仍可讀舊schema 3。
- 新增本機工具產生JSON／Markdown小表，CLI輸出只含數量。輸出限repo已忽略的tmp，不寫OneDrive或Google Drive。

### 私人表及驗證結果

- `tmp/product-review-20260918/product-catalog.json`及`PRODUCT_CATALOG.md`：11個既有確認型號、1個大類、135種待核對寫法。
- 已確認區保留大小寫／空格等實際寫法，按不同Serial計數；同設備的多筆PM不重複加設備數。不含設備序號、task ID或订单号。
- 排除統計按「設備列×原寫法」計數，不是不同task數：2,035個Serial形狀、21個長數字識別碼、10個非產品格式、2個工作參考文字。489個弱身份設備列不參與設備頻率統計。
- 本機既有20筆独立Asana證據快照：19筆提取出已確認產品寫法，1筆額外後綴寫法標為未確認。這是標題解析檢查，不是20份工作單匹配成功率，也沒有解決跨頁原件衝突。
- 新增12個合成測試，完整測試215個通過；涵蓋污染欄、CM型號、未知產品、型號衝突、同段Serial、位置隔離、不同設備計數、頻率不認證、私表讀回及舊版本完整重建要求。
- 沒有真實OCR／Asana呼叫、雲端寫入、提交或推送。小表尚未接入視覺模型；舊索引沒有完整任務標題，不能靠本機小表修復全部設備的錯誤歸屬。

### 下一步

4B：用乾淨且可核實的設備資料統計產品對應Serial長度／前綴／字母數字位置，至少三部不同設備支持才作常見提示；不從污染欄或同一設備重複工作推規律。加入產品格與Serial格聯合閱讀，再以相同圖片比較有無知識提示。正式重建／推送仍待最後驗收與確認。

## 2026-09-18：4B 聯合讀取與對照準備（尚未完成真實對照）

### 已完成

- `src/vision_knowledge.py`只接受新產品解析版本的索引。按不同Serial統計產品大類、序號長度、字母／數字位置與字母前綴；同設備多筆PM／CM及重複列不增加機器數。
- 至少3部不同設備才列作常見格式；只保留共同字母位置，數字位置永不學成固定數字，不提供完整Serial範例。少見格式仍保存在私人統計中，但不作強制規則。
- 排除弱身份、產品未確認、同Serial產品大類衝突、task參考Serial不符及舊污染索引。模型只收到白名單投影，最多40種常見格式，不會收到task答案、電話、asset或來源自由文字。
- 新增隔離的 `read_joint_identity_image`：產品格與Serial格並排，医院另格；可選已確認產品、醫院簡寫及統計格式提示。只憑清楚筆跡抄寫、允許 `?`、少見寫法不等於錯，禁止用規律補完整序號。
- 返回 `transcription` 與 `assisted`，各自保存原始／整理結果，辅助結果不覆蓋原文，也不算第二次獨立共識。尚未接入正式Stage B。
- 額外修正安全邊界：含 `?` 的Serial不能透過刪掉問號變成另一個合法序號；只保留在診斷原文，不進正式或視覺Serial候選。
- `src/vision_experiment.py`提供同一張圖片的無提示／有提示對照，圖片只渲染一次，模型相同且不切後備。每組各一次呼叫、可交換先後次序；失敗不無限重試，日誌不打印異常中的客戶原文。
- 實驗結果只寫本機tmp，不能覆蓋既有結果；成功收到回覆也標 `NOT_SCORED`，不假冒準確率。CLI須明確允許模型呼叫，並驗證排除測試設備的知識檔與PDF指紋一致。

### 私人資料準備與限制

- 只採用先前獨立Asana讀取、私人答案已確認的18筆工作，共16部設備；沒有拿4,596列舊污染索引直接學規律。
- 目前完整小樣本有1種常見格式達3部機器門檻（Affiniti大類，10位、USN前綴、字母／數字結構）；另有10種少見格式。不能據此宣稱所有EPIQ或其他產品的序號規律已掌握。
- 已為20份PDF各製作知識檔，排除該份紙上讀到及已確認Asana設備的全部歷史工作；排除後某些格式不足3部便不提供模型。
- 私人來源指紋、知識總覽和20份知識檔在 `tmp/joint-review-20260918/`；準備腳本在 `tmp/prepare_joint_experiment.py`，被Git忽略。
- 按PDF檢查流程重新渲染並人工查看B01、B16、B18的聯合圖，產品／Serial及醫院值可見；只是圖片驗證，不是模型辨認結果。
- 本機環境目前沒有NVIDIA或DeepSeek key。只執行不呼叫模型的preflight，回傳 `READY_NOT_RUN`；未查證GitHub secret狀態、未改供應商、未發生API費用。

### 驗證與下一步

- 新增14個合成測試，完整測試229個通過；涵蓋不同設備計數、整台设备留出、衝突排除、舊索引拒絕、知識白名單、數量上限、`?`安全邊界、原文／辅助分離、同圖對照、失敗不洩漏及PDF綁定。
- 真實有無提示對照、欄位準確率及整體20份回測尚未執行；需要安全注入現有模型密鑰，或獲准後用雲端隔離測試。不要把密鑰貼聊天或寫入Git。
- 未提交／推送，未改自動流程，未寫OneDrive或Google Drive。仍須完成Asset／日期規則及真實回測，才能決定啟用新提示。

## 2026-09-18：5A Asset輔助分（本機完成）

### 改動及邊界

- 新增共用Asset判準：只比較數字，至少4位；相似率為 `1 - 編輯距離 / 較長長度`，整數百分比四捨五入至少70%才有模糊加分。使用整數運算避免浮點邊界及Python偶數捨入差異。
- 設備排名、索引歷史task預選、即時Asana task判分全部共用。完全相同強於模糊相似，空白／缺失／不相符均為零分，沒有扣分或新增排除條件。
- 最佳一組Asset證據只計一次，多個OCR候選及重複歷史資料不疊加分數。公開日誌只記證據種類，沒有Asset原文。
- 有可辨認Serial的候選不要求Asset；Serial完全讀不到時，舊版「產品、醫院、完整電話及精確Asset唯一命中」例外暫未改動。模糊Asset不得冒充精確身份救援。這一步不宣稱已完成整個第五步。
- 設備候選原有Serial分差安全閘仍生效；同分歷史工作不因模糊Asset便隨意選一個。電話判準沒有跟著放寬成70%。

### 驗證

- 新增13個純合成資料測試，完整測試242個通過（原229個）；包括70%／低於70%、四捨五入邊界、缺失與不符零扣分、精確分高於模糊、三層一致、歷史資料不疊加、無Asset仍配對、歷史工作並列及日誌保密。
- 首次新增測試中兩個測試字串計錯了實際替換字數，已修正測試資料；未為配合測試而更改70%規則。
- 沒有呼叫模型或真實Asana，沒有API費用；沒有雲端寫入、搬移來源、提交或推送。242個程式測試不是20份實單的準確率。

### 下一小步：5B 日期與工作選擇

- 檢查發現完整task的舊 `_task_dates` 仍把描述日期混入，未以正式到期／完成日為一致判準；舊 `_service_date_delta` 亦可自動替換年份。它們與新計劃不一致，這次先記錄，尚未更改。
- 下一步須統一索引預選與最終task核對的日期來源，阻止描述舊日期／自動換年影響工作選擇，先確認設備和CM/PM，再選具體工作。
- 可靠ACTION DATE優先；日期不能可靠讀取時，僅可使用有來源的原始上傳／掃描日期前70日至後30日，不使用今天或Asana修改時間。明確衝突、未知來源或並列仍保留待核對。
- 目前20份私人樣本的原始上傳日期均未知，不能為了跑通而補今天。4B真實有無知識提示對照亦仍未執行。

## 2026-09-24：產品／醫院單格方案先接入隔離回測

- 9張私人掃描的兩次完整獨立對照，產品及醫院單格的有限詞彙提示各為9/9；
  這只評估兩個欄位，不能當作20份設備／具體工作／檔名都正確。
- 測試分支加入 `JOBSHEET_CONTEXT_FIELD_OCR=1`；20份只讀回測才開此開關。
  正式Stage B及main均未啟用。
- 產品與醫院各以相同的5x單格提示獨立讀兩次。原先大欄位卡的原文仍留在
  私人診斷，但不能給這兩欄投票；任一欄未形成兩次共識便不查Asana、不上傳。
- 提示共用同一函數，避免實驗用過的文字與回測實際用的文字分叉。詞彙
  由私人設備索引在執行時投影：只取已確認的產品寫法及醫院短碼，排除
  Serial、電話、聯絡人、Asset、詳細地點和工作編號，不把客戶表提交Git。
- 因詞彙來源由硬寫清單改為私人索引，先前9張9/9不能自動沿用；必須重新
  以新提示測試，再開始20份全流程只讀回測。
- 目前僅完成本機程式接線及合成測試；重建私人索引和20份實單只讀回測仍待執行。

## 2026-09-25：隔離分支實測與日期防錯閘

- 私人索引投影提示曾在9份對照中得到產品9/9、醫院8/9；加入「完整醫院名稱必須逐字抄寫，不可被簡寫取代」後，同組再測為產品9/9、醫院9/9。此結果只涵蓋兩欄。
- 測試分支完整重建近兩年私人索引，輸出只放在 Google Drive 私人控制目錄的測試子資料夾，未覆蓋正式索引：104個project、9,198個task、4,364部設備、275組地點。
- 使用新索引跑20份只讀回測：嚴格正確11/20，未獨立確認答案1份，缺頁3份（其中1份僅作診斷）；圖片服務錯誤0、OneDrive寫入0。完整且答案已確認的16份中，11份正確、4份未選出工作、1份選錯同設備的具體工作。
- 失敗主要在歷史工作選擇：同設備多次PM而日期／電話證據不足會安全保留待確認；另有一份日期分數令錯月份工作勝出，故**正式流程不可啟用此改版**。
- 本機新增防錯閘：多筆歷史工作若只靠相差超過3天的日期分數分開，且沒有能區分兩筆工作的完整電話、Asset或工單號，就不自動選。新增2個合成測試，完整測試264個通過。這只證明安全邏輯，不是實單回測已改善。
- 防錯閘仍只在本機，尚未推測試分支：公開repo的 `src/asana_client.py` 含硬寫的客戶／聯絡資料，雲端提交被安全檢查拒絕。不可用另一個提交途徑繞過；下一步先清理這份程式中的敏感常量，再在隔離分支對錯月份樣本做只讀重測，合格後重跑20份。正式main及OneDrive未改。

### 中斷後接續（仍為本機，未重跑實單）

- 已移除 `src/asana_client.py` 裏寫死的完整醫院名稱和電話／聯絡人例子。程式只保留既有確認短碼；完整院名與模糊錯字校正從執行時載入的私人 `location_directory` 取得。未確認短碼不因此變成合法候選。
- 重建地點表時，Asana 只給短碼就只記短碼；若同組實際出現完整名稱，才以原文作顯示名稱。這避免公開程式碼代替私人資料編造正式名稱。
- 最終即時 Asana task 的日期現在先用正式到期日／完成日；兩者都沒有時才看描述中的日期，與索引預選規則一致。原有 B08 防錯閘仍在本機。
- 加入 7 個不含客戶資料的合成測試，完整自動測試 271 個通過；這不是 B08 或 20 份真單準確率。
- 已核對 Google Drive 測試子資料夾仍為私人狀態；沒有改動任何檔案。嘗試把清理後程式交給 GitHub 外掛時，當前環境要求寫入批准但批准不可用，因此**未上傳、未提交、未觸發雲端回測**。不可改用其他上傳途徑繞過拒絕。正式 main、OneDrive 及正式設備索引完全未動。
- 其後檢查 GitHub 外掛：連線正常，但目前繼承「只自動允許低風險操作」設定；本次寫入仍需額外批准。未自行更改外掛權限。
- 為避免將舊測試中的真實風格醫院／電話／序號／訂單例子重新提交，已把本次須改動的三個測試檔改用清楚的合成資料，並保留原測試的相似度與錯字關係；完整 271 個測試再次通過。這是本機保密整理，仍不是雲端 B08／20份回測。
- 再核對 B08 私人原件與答案：工作單日期與較早的一次正式 Asana 工作一致；上次回測錯選同設備之後的工作。此核對只確認舊錯誤，不等於新模型已跑通。
- 補上日期預選的邊界：同設備近三個月有另一筆同類工作時，即使該工作在索引日期篩選階段被排除，仍不能讓相差超過三日且無獨立工作證據的唯一剩餘候選自動命名。久遠歷史工作不會因此阻擋正常配對。新增兩個合成檢查，完整測試 273 個通過；B08／20份雲端實單仍未重跑，OneDrive 寫入為零。
- 隔離回測 workflow 下一次只選 B08；先確認舊錯月份不再自動通過，再決定是否重跑全部20份。只改測試分支的工作，正式 Stage B 及 main 不受影響；目前未推送，所以這次 B08 雲端驗證尚未發生。

### B08 隔離雲端回測及下一輪單格日期實驗

- 已把上述保密整理及日期防錯閘推到獨立測試分支。雲端程式測試通過；B08 只讀回測執行成功但品質結果仍為失敗：錯月份工作被攔下，沒有選出正確工作，OneDrive 寫入 0。這不是整體 20 份合格。
- 人工看原 PDF 的 ACTION DATE 小格，未被裁掉；筆跡中的月份容易與另一數字混淆。兩張大欄位卡即使讀成同一個錯月份，原有「兩次一致」也不足以證明日期正確。
- 因此在隔離回測開關下，當歷史工作仍未能確定而已有日期共識時，再對 ACTION DATE 小格以兩種放大影像各讀一次。兩次單格結果都有效且同日才覆蓋原大卡日期並重找工作；任何一輪失敗或兩輪不一致，就撤銷日期證據並留待核對。原始四輪讀數只留私人診斷，不把客戶資料寫入公開日誌。
- 本機 276 個合成／程式測試通過；接下來仍只對 B08 做一次只讀雲端回測。若 B08 仍不合格，不擴大到 20 份，也不啟用正式 Stage B。
- 日期單格兩輪雲端實測仍未把 B08 選對，錯工作繼續被攔住且 OneDrive 寫入 0。使用者確認原件月份為 8；即時只讀 Asana 亦顯示較早那次工作已於當天完成並記錄原單上的探頭資訊，較新的同設備工作未完成且沒有這些資訊。
- 檢查渲染圖發現「單格複核」的上邊界切掉手寫日期的上緣。只把日期複核裁切起點恢復到原完整欄位的上邊界，補一個合成圖測試確保上緣筆劃保留；暫不改日期提示詞、Asana 判分或正式流程。下一次 B08 只讀回測只檢查這一個影像修正。
- 上述影像修正後再次只讀回測 B08，仍未能選出正確工作；模型的日期結果仍導向另一個月份，防錯閘保留待核對，OneDrive 寫入 0。雲端程式檢查通過；本機 277 個測試通過。補全影像有必要，但單靠這個改動不能修正該手寫字。
- 即時只讀 Asana 顯示兩次 PM 的電話／Asset 仍不足以分開。原件 ACTION TAKEN 的多個探頭識別碼只見於正確月份工作的描述，另一筆近期工作沒有；這可能是更強的「哪一次工作」證據，但早前規則排除維修內容參與命名，故**尚未實作或啟用**，先請使用者確認是否同意在隔離回測使用。
- 使用者同意**只在獨立回測**試用 ACTION TAKEN 的探頭／零件識別碼。模型不看 Asana 候選，5x/6x 各抄一次；只保留兩輪相同、同時含字母和數字的至少兩個識別碼。程式只與即時 Asana task 描述作完整 token 比對，不用標題或設備列的歷史混合資料。兩次工作都有相同識別碼、只有一個碼或沒有索引時仍不自動選。
- 此輔助欄位只在 `JOBSHEET_CONTEXT_FIELD_OCR=1` 且先前找不到唯一工作時才讀；正式 Stage B 沒開該旗標，故尚未受影響。公共日誌只記是否取得足夠證據，不印任何識別碼。284 個本機合成測試及編譯通過，B08 實單雲端結果仍待驗證；達標前不擴大到 20 份或啟用正式流程。
- B08 雲端只讀回測已通過嚴格驗收：正確設備、當次 Asana 工作及預計名稱一致；新證據來自兩輪同意的 ACTION TAKEN 識別碼，且只有正確工作描述具備足夠吻合。15 次圖片呼叫，費用保守上限約人民幣 0.0335，OneDrive 寫入 0。這仍只是 1/1，不代表其餘 19 份正確。
- 下一步把同一隔離 workflow 從 B08 單份限制恢復為 20 份只讀回測，確認新證據不會產生錯配。結果未達標前，正式 Stage B 和 main 保持不變。

### 20 份只讀回測結果（2026-09-25）

- 獨立測試分支已重跑 20 份，嚴格正確仍為 **11/20**：11 份通過、7 份未通過、1 份答案未獨立核實、1 份僅作缺頁診斷。圖片呼叫 278 次、累計約 340 秒、費用保守上限約人民幣 0.6019；OneDrive 寫入 0。
- 與上次回測逐份對照：B08 由選錯同設備工作改為正確；B01 由通過改為安全待確認，其餘樣本狀態不變。這是兩個變化互相抵銷，不可描述成整體準確率提升。
- B01 的原件 ACTION DATE 與正確 Asana 工作正式日期接近，但模型多輪讀數未穩定；同設備有多次 PM，日期防錯閘不准只靠相差超過三天的日期分數作決定。ACTION TAKEN 的兩輪抄錄亦未提供足以獨立區分該次 Asana 工作的證據，所以本輪未自動命名。
- 其餘四份完整未配對各有不同原因：B03 能縮到一部設備但兩次歷史工作未能唯一分開；B04 同設備有多次工作且最終分數並列；B07 的 Serial 讀數有兩字差異，剩餘工作亦並列；B10 的 Serial、電話及日期都未形成可靠共識，兩次工作無法分開。這些是匿名判斷原因，不在 Git 記錄客戶原文。
- 五份完整、答案已確認而未通過的樣本均未選出工作，沒有發現錯誤自動配對；另兩份未通過樣本屬缺頁。B05 仍未獨立核實，B18 只作缺頁診斷。這只評估配對和預計名稱，並非切頁或 OneDrive 正式驗收。
- 程式檢查及本機 284 個測試通過；**未達 20 份驗收標準，不啟用正式 Stage B，不推 main**。下一步須針對 B01 和其餘四份完整未配對樣本分辨是日期讀取、當次工作資料不足，還是過嚴的判分；不能為了分數移除防錯閘。

### 五份未配對樣本的共同日期交叉核對（隔離測試中）

- 初步假設兩個簽署日期都可核對 ACTION DATE，模型不看 Asana 日期或候選；其後實單檢查發現工程師可早幾天簽，故「兩簽署日期必須一致」的初版規則已廢止，以下實測段落記錄修訂。
- 已交叉確認的 ACTION DATE 可在同設備歷史工作判分中更強地區分月份；同日期的兩次工作仍不能靠日期決勝。正式 Stage B 沒開此回測旗標，不受影響。
- 本機 290 個程式測試通過；接下來先只讀回測 B01、B03、B04、B07、B10。五份未全部正確前，不重跑20份，更不啟用正式流程。

### 2026-09-25：五份實單的日期規則實測（未達標）

- 獨立分支做了三次五份只讀回測，均未達五份全對；之後另做四份日期診斷。所有執行均沒有 OneDrive 寫入，沒有搬移 Google Drive 正式來源，也沒有更改 main 或正式 Stage B；因此**沒有啟動全部20份回測**。
- 原先「工程師、客戶兩個簽署日期必須相同」的假設不成立：私人原件顯示工程師可能早幾天簽。已改為以 ACTION DATE 為主，客戶簽署日期只作獨立核對；印章遮住日數時，只嘗試用兩次完整 ACTION DATE 與兩次客戶月份／年份作保守核對。
- B03 在第三次五份回測已選對設備及當次 Asana 工作，但檔名未通過：人工答案表記載該工作沒有訂單號，現時即時 Asana 標題卻已有訂單號。未擅自改答案表，待使用者決定依現行 `SR#` 規則還是沿用較早指定的標題名稱。
- B01、B04、B07、B10 在最近一次四份回測仍安全地留待確認，沒有錯誤自動配對。匿名診斷顯示：模型能讀出若干手寫日期，但同一數字有穩定誤讀；B04 客戶簽署日被印章遮擋，完整日期不可讀。放鬆選擇門檻無法修復來源讀值，故停止擴大規則。
- 本機 300 個程式測試通過，這只證明防錯與接線，不代表真實 OCR 正確。下一步應先取得使用者同意，於獨立分支對同五份做另一視覺模型的**只讀對照**，不更換正式 OCR；如仍未達標，須決定是否保留人工核對這類難讀日期。正式批量處理繼續暫停。

### NVIDIA 五份只讀對照（2026-09-25，未達標）

- 使用者同意後，只把獨立測試 workflow 的五份樣本設為 B01、B03、B04、B07、B10，這次選 NVIDIA；正式 Stage B 模型、main、OneDrive 及 Google Drive 正式佇列均未改動。雲端程式檢查和回測 workflow 本身成功，但嚴格配對 **0/5**，五份都安全地未選出工作，沒有錯誤自動上傳。
- 匿名結果共 128 次圖片呼叫、約 260 秒模型等待、385,902 tokens；NVIDIA 模型沒有在此報告中估價，不能把零或空值當免費。三份樣本曾遇主模型無回應並切到既有後備，故本輪是「NVIDIA 供應鏈」對照，不是純單一模型能力測試。
- 匿名診斷顯示五份都有產品／醫院／Serial 相似的設備候選；回測的 `device_check=FAIL` 是因最終 task 為空，**不等於完全找不到設備**。B01 的前兩設備過近，受限複核也未能確定；B03、B04 的單格 ACTION DATE 兩輪不一致，簽署日期亦不能安全修正；B07、B10 的歷史工作無法唯一分開，日期複核分別超出可信範圍或無共識。
- 對比先前 DeepSeek 五份回測：該輪 B03 至少選對設備及當次工作，僅檔名答案與當前 Asana 訂單號衝突；NVIDIA 本輪連 B03 工作也未選出。B03 命名答案維持待使用者確認，不因模型回測自動修改。兩種供應鏈皆未令五份全對，因此**不啟動全部 20 份回測，也不部署正式流程**。
- 下一步應針對「前兩設備接近」和「同設備多次工作日期難讀」分別設計可驗證的證據，不應把所有 `device_check=FAIL` 都當成產品／醫院辨認錯誤，也不應單靠改模型或降低安全門檻。

### 更正答案後的五份 DeepSeek 只讀回測（2026-09-25，未達標）

- 使用者確認 B03 應依目前 Asana 訂單號採 `SR#` 命名；新私人答案另存，不覆蓋舊清單，也不把訂單號或客戶資料寫進 Git。
- 隔離分支增加「設備候選排名」與「最終是否選出設備／工作」的分項報告；沒有選出工作時標記 `NOT_SELECTED`，不再誤報成設備辨認錯誤。雲端程式檢查及本機 302 個自動測試通過。
- [五份只讀執行](https://github.com/qstuer/jobsheet-automation/actions/runs/36149586044)：嚴格配對 **1/5**，B03 的設備、當次工作、檔名和 PM 類型全對；B01、B04、B07、B10 安全地沒有選出工作。五份的正確設備均排在索引第一名，圈選均正確，OneDrive 寫入 0。此回測不驗證逐頁 checklist 或正式上傳。
- 四份未通過的共同瓶頸是同設備多筆歷史工作無法由不穩定的手寫日期及當次輔助證據唯一分開；不能再把這四份歸因於找不到產品或醫院。B04、B10 的 ACTION DATE 多輪未形成可用日期；B01、B07 的日期／工作證據仍衝突或並列。
- 以私人索引實算「日期讀不到就選最接近回測當天」：五份中會選對三份、選錯兩份，故不可作自動命名規則。五份 PDF 均沒有可信的掃描／上傳時間中繼資料，也不能用本機修改時間代替原始上傳日。
- **不啟動全部 20 份回測、不推 main、不改正式 OCR 或 OneDrive**。下一步須在隔離測試中找能獨立核對 ACTION DATE 的方法；不得以降低配對門檻換取表面通過率。

